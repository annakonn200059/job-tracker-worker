[project]
name = "job-tracker-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "konnova.anya2016@yandex.ru" }
]
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "structlog>=26.1.0",
]

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "src"
module-name = "worker"